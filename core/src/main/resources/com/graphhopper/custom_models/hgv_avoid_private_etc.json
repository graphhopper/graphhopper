{
  // Note, 'turn_penalty' requires enabled turn_costs in profile
  "turn_penalty": [
    {
      "if": "prev_hgv_road_access != hgv_road_access && (hgv_road_access == DESTINATION || hgv_road_access == PRIVATE || hgv_road_access == DELIVERY || hgv_road_access == CUSTOMERS || hgv_road_access == MILITARY)",
      "add": "2000"
    }
  ]
}
