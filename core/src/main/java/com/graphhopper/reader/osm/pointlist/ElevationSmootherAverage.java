package com.graphhopper.reader.osm.pointlist;

import com.graphhopper.util.PointList;
import org.codehaus.commons.nullanalysis.NotNull;

public class ElevationSmootherAverage implements PointListProcessor {
    @Override
    public @NotNull PointList process(@NotNull PointList pointList) {
        return null;
    }
}
