<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <artifactId>graphhopper-tools</artifactId>
    <packaging>jar</packaging>
    <name>GraphHopper Tools</name>

    <parent>
        <groupId>com.graphhopper</groupId>
        <artifactId>graphhopper-parent</artifactId>
        <version>12.0-SNAPSHOT</version>
    </parent>
    <properties>
        <assembly-phase>package</assembly-phase>
        <!-- must match core/pom.xml: tools compiles the build-time generated Kotlin weighting -->
        <kotlin.version>2.4.0</kotlin.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.graphhopper</groupId>
            <artifactId>graphhopper-core</artifactId>
            <version>${project.parent.version}</version>
        </dependency>
        <dependency>
            <groupId>com.graphhopper</groupId>
            <artifactId>graphhopper-web-api</artifactId>
            <version>${project.parent.version}</version>
        </dependency>

        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-compress</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-core</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>
        <dependency>
            <groupId>com.graphhopper.external</groupId>
            <artifactId>jackson-datatype-jts</artifactId>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
        </dependency>
    </dependencies>
    <build>
        <plugins>
            <!-- kotlinsource weighting backend (measurement.custom_weighting_backend=kotlinsource):
                 generate build-compiled Kotlin CustomWeightings from the benchmark's custom models
                 (core's car.json + benchmark/very_custom.json) so the benchmark can route with the
                 AOT/no-runtime-compiler path. The generated .kt files are THROWAWAY build artifacts
                 under target/ (gitignored, regenerated from the models every build - never committed,
                 never stale). This adds one codegen step + a Kotlin compile pass to every tools build.
                 encoded_values is the UNION of both models' referenced EVs. -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.1.1</version>
                <executions>
                    <execution>
                        <id>generate-registry-weighting</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>java</goal>
                        </goals>
                        <configuration>
                            <mainClass>com.graphhopper.routing.weighting.custom.generate.GenerateCustomWeightingMain</mainClass>
                            <arguments>
                                <argument>custom_models=${project.parent.basedir}/benchmark/car_access_speed.json,${project.parent.basedir}/core/src/main/resources/com/graphhopper/custom_models/car.json,${project.parent.basedir}/benchmark/very_custom.json</argument>
                                <argument>encoded_values=car_access,car_average_speed,road_environment,max_speed,ferry_speed,road_class,road_access,toll,hazmat,max_height,max_width</argument>
                                <argument>output=${project.build.directory}/generated-sources/customweightings</argument>
                                <argument>package=com.graphhopper.tools.weighting.generated</argument>
                            </arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- compile the generated Kotlin before javac (mirrors core/pom.xml); the generated
                 dir is the only Kotlin source root, tools' own sources stay Java -->
            <plugin>
                <groupId>org.jetbrains.kotlin</groupId>
                <artifactId>kotlin-maven-plugin</artifactId>
                <version>${kotlin.version}</version>
                <executions>
                    <execution>
                        <id>compile</id>
                        <phase>process-sources</phase>
                        <goals>
                            <goal>compile</goal>
                        </goals>
                        <configuration>
                            <sourceDirs>
                                <sourceDir>${project.build.directory}/generated-sources/customweightings</sourceDir>
                                <sourceDir>${project.basedir}/src/main/java</sourceDir>
                            </sourceDirs>
                        </configuration>
                    </execution>
                </executions>
                <configuration>
                    <jvmTarget>${maven.compiler.target}</jvmTarget>
                </configuration>
            </plugin>
            <!-- the default useIncrementalCompilation=true wipes the whole output directory on
                 stale sources (MCOMPILER-209), deleting the classes kotlinc just wrote -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <useIncrementalCompilation>false</useIncrementalCompilation>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-assembly-plugin</artifactId>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>com.graphhopper.tools.Measurement</mainClass>
                        </manifest>
                    </archive>

                    <!-- for standalone usage -->
                    <descriptorRefs>
                        <descriptorRef>jar-with-dependencies</descriptorRef>
                    </descriptorRefs>
                </configuration>
                <executions>
                    <execution>
                        <id>make-assembly</id>
                        <phase>${assembly-phase}</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
    <profiles>
        <profile>
            <!-- use -Ptools-jar to skip building the tools jar with dependencies -->
            <id>skip-tools-jar</id>
            <properties>
                <assembly-phase>none</assembly-phase>
            </properties>
        </profile>
    </profiles>
</project>
