{
  // Note, 'turn_penalty' requires enabled turn_costs in profile
  "turn_penalty": [
    {
      "if": "prev_foot_road_access != foot_road_access && foot_road_access == PRIVATE",
      "add": "2000"
    }
  ]
}
